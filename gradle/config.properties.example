# Android Studio reads this from .gradle/config.properties (not gradle/).
# Copy to .gradle/config.properties if sync fails with invalid Gradle JDK.
java.home=C:/Program Files/Android/Android Studio/jbr
